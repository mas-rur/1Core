# Get a free project ID at https://cloud.reown.com (formerly WalletConnect Cloud)
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Get a free API key at https://dashboard.0x.org
# Server-side only — never exposed to the browser.
ZEROX_API_KEY=

# Optional: override the default 0x API base (defaults to https://api.0x.org)
ZEROX_API_BASE=https://api.0x.org
